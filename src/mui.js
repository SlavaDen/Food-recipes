import { createTheme } from "@mui/material";

const theme = createTheme({
  typography: {
    fontFamily:
      "LatoBold, -apple-system, BlinkMacSystemFont, Segoe UI, sans-serif",
  },
});

export { theme };
